# This is the new, correct build file for your Android app

name: Build Android App

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. Checks out your repository's code
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. Sets up the Java (JDK) environment
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    # 3. THIS IS THE NEW STEP: Sets up the Android SDK
    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    # 4. Gives permission to the 'gradlew' file to run
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # 5. This command builds the debug .apk file
    - name: Build with Gradle
      run: ./gradlew assembleDebug

    # 6. This saves the finished .apk file so you can download it
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug.apk # This will be the name of your download
        path: app/build/outputs/apk/debug/app-debug.apk
